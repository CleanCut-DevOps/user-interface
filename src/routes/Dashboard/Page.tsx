import { FC } from "react";
import { DefaultLayout } from "../../components";

export const Dashboard: FC = () => {
    return (
        <DefaultLayout>
            <>Dashboard</>
        </DefaultLayout>
    );
};
