import { FC } from "react";

import { Booking } from "~/models";

interface ComponentProps {
    bookings: Booking[];
}

export const BookingsPanel: FC<ComponentProps> = ({ bookings }) => {
    return <>component</>;
};
